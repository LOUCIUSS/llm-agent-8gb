# ============================================================
# 8GB LLM Agent - Phase 01: Environment Setup Requirements
# ============================================================
# Target: Python 3.10+ | OS: Linux (Ubuntu/Debian)
# Estimated total install size: ~2-3 GB
# ============================================================

# --- Core Inference (1.1-1.2) ---
# llama-cpp-python with OpenBLAS for CPU-optimized inference
# Install with: CMAKE_ARGS="-DLLAMA_BLAS=ON -DLLAMA_BLAS_VENDOR=OpenBLAS" pip install llama-cpp-python
llama-cpp-python>=0.3.0,<0.4.0

# --- Embeddings (1.3) ---
# High-quality sentence embeddings (downloads ~400MB for nomic-embed-text)
sentence-transformers>=3.0.0

# --- API Server Stack (1.4) ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
sse-starlette>=2.1.0

# --- Web Search & HTTP (1.4) ---
duckduckgo-search>=6.3.0
requests>=2.32.0
beautifulsoup4>=4.12.0

# --- System Utilities (1.4) ---
psutil>=6.1.0

# --- Vector Store & Document Processing (1.5) ---
qdrant-client>=1.12.0
pymupdf>=1.24.0           # PyMuPDF for PDF processing
python-docx>=1.1.0
pytesseract>=0.3.13
rank-bm25>=0.2.2
huggingface_hub>=0.26.0

# --- Vision & Image Processing (1.6) ---
Pillow>=11.0.0

# --- CLI & Logging (1.6) ---
rich>=13.9.0
prompt_toolkit>=3.0.48
typer>=0.13.0
loguru>=0.7.0

# --- Configuration & Utils (1.6) ---
PyYAML>=6.0.2
pytest>=8.3.0
numexpr>=2.10.0

# --- Fine-tuning Stack (Phase 10 - install when needed) ---
# transformers>=4.46.0
# peft>=0.13.0
# trl>=0.11.0
# bitsandbytes>=0.44.0
# datasets>=3.1.0
# accelerate>=1.1.0

# --- Optional GPU support (uncomment if CUDA available) ---
# torch>=2.5.0
# torchvision>=0.20.0
